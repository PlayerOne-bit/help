# Let's Create an App Tutorial:

First, backend we should learn Node.js + Express.js
[Install Node First](https://nodejs.org/en/download)

Note: To run npm, git, or any commands in any IDE with terminal...
Go to environment variable, paste the path of the installed command to the user variables.

So In the terminal of your [Visual Studio Code](https://code.visualstudio.com/download?_exp_download=d53503e735) to create a package.json run this command:
`npm init`